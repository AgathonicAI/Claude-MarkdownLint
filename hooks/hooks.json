{
  "description": "Markdownlint plugin hooks: auto-setup and proactive linting",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh",
            "timeout": 120
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If the edited file ends with .md, lint it with lint_markdown. Report issues briefly and offer to fix. Silent if not markdown or no issues."
          }
        ]
      }
    ]
  }
}
